import SwiftUI

struct HomeView: View {
    var body: some View {
        Text("Home — Coming in Sprint 2")
            .navigationTitle("Home")
            .navigationBarTitleDisplayMode(.large)
    }
}
