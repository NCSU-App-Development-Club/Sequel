import SwiftUI

struct ProfileView: View {
    var body: some View {
        Text("Profile — Coming in Sprint 4")
            .navigationTitle("Profile")
            .navigationBarTitleDisplayMode(.large)
    }
}
